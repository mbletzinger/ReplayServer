/**
 * Replay Server Common Restlet
 * This package provides the restlet classes which define the replay server.  These include resource classes to manage
 * REST requests as well as an application class and component class.  The resources, application, and component are
 * configured with a GUICE dependency injector so that they can be create as either a test configuration or the
 * production server.
 */
/**
 * @author Michael Bletzinger
 *
 */
package org.nees.illinois.replay.restlet;

